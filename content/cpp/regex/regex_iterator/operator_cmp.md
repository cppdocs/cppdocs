---
title: "std::regex_iterator<BidirIt,CharT,Traits>::operator==,operator!="
source_path: "cpp/regex/regex_iterator/operator_cmp"
category: "regex"
since: "C++11"
---

Compares two regex_iterators.

## Declarations
```cpp
bool operator==( const regex_iterator& rhs ) const;
```
_(since C++11)_

```cpp
bool operator!=( const regex_iterator& rhs ) const;
```
_(since C++11) (until C++20)_

## Parameters
- `rhs`: a regex_iterator to compare to

## Return value
For the sake of exposition, assume that regex_iterator contains the following members:

## Example
This section is incompleteReason: no example
